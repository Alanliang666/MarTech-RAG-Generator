version: '3.8'

services:
  api:
    build: .
    env_file: .env

    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data

  worker:
    build: .

    env_file: .env
    command: celery -A src.worker worker --loglevel=info
    volumes:
      - ./data:/app/data
